RewriteEngine On

# Redirect root domain to public directory
RewriteCond %{HTTP_HOST} ^(www\.)?darkturquoise-herring-431565\.hostingersite\.com$ [NC]
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]

# Optional: Redirect www to non-www (or vice versa)
# RewriteCond %{HTTP_HOST} ^www\.darkturquoise-herring-431565\.hostingersite\.com$ [NC]
# RewriteRule ^(.*)$ https://darkturquoise-herring-431565.hostingersite.com/$1 [R=301,L]

# Optional: Force HTTPS
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Security headers (optional)
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# Cache control for static files (optional)
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/pdf "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    ExpiresByType application/x-shockwave-flash "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresDefault "access plus 2 days"
</IfModule>
<FilesMatch "\.pdf$">
    ForceType application/pdf
    Header set Content-Disposition inline
</FilesMatch>