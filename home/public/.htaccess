RewriteEngine On

# Prevent directory listing
Options -Indexes

# CodeIgniter 4 - Remove index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]